apply plugin: 'java'

group = 'com.github.alemures.fasttcp'
version = '1.0.1'

targetCompatibility='1.7'
sourceCompatibility='1.7'

jar {
    baseName = project.name
    version =  project.version
}

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.12'
}
